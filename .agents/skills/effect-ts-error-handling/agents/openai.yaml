interface:
  display_name: "Effect-TS Error Handling"
  short_description: "Preserve parse errors, typed failures and full Causes"
  default_prompt: "Use $effect-ts-error-handling to classify parsing, absence, expected failure, defects and interruption, then implement or review precise recovery, per-attempt retry eligibility and lifecycle-safe boundaries."