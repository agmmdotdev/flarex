{
  "name": "@flarex/example",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm generate && vite build",
    "generate": "tsx scripts/generate.ts",
    "typecheck": "pnpm generate && tsc -p tsconfig.json",
    "test": "pnpm generate && vitest run"
  },
  "dependencies": {
    "flarex": "workspace:*"
  },
  "devDependencies": {
    "flarex-backend": "workspace:*",
    "miniflare": "4.20260611.0",
    "tsx": "^4.21.0"
  }
}
