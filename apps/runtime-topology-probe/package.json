{
  "name": "@flarex/runtime-topology-probe",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm typecheck",
    "deploy:gateway:dry-run": "tsx scripts/runWranglerDryRun.ts gateway",
    "deploy:mock:dry-run": "tsx scripts/runWranglerDryRun.ts mock",
    "deploy:sync:dry-run": "tsx scripts/runWranglerDryRun.ts sync",
    "test": "vitest run --no-file-parallelism --test-timeout=20000",
    "test:local": "vitest run test/gateway.miniflare.test.ts --no-file-parallelism --test-timeout=20000",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "catalog:",
    "miniflare": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
