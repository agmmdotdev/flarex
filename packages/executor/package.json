{
  "name": "@flarex/executor",
  "version": "0.0.1",
  "type": "module",
  "files": [
    "src"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:postgres": "vitest run test/postgresRetry.test.ts"
  },
  "dependencies": {
    "@flarex/freshness": "workspace:*",
    "@flarex/persistence-postgres": "workspace:*",
    "flarex": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "pg": "^8.22.0",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
