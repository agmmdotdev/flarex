{
  "name": "@flarex/managed-schema",
  "version": "0.0.1",
  "type": "module",
  "files": [
    "src"
  ],
  "exports": {
    "./candidate-document": "./src/CandidateDocument.ts",
    "./compatibility": "./src/Compatibility.ts",
    "./planning": "./src/Planning.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check:boundary": "pnpm --dir ../.. exec node scripts/check-managed-schema-boundary.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test": "pnpm check:boundary && vitest run"
  },
  "dependencies": {
    "@flarex/utils": "workspace:*",
    "effect": "catalog:",
    "flarex-protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
