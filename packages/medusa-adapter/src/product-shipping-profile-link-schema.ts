import { ProductShippingProfile } from "@medusajs/link-modules";
import { captureLinkMetadata } from "./link-schema";
export const captureProductShippingProfileLinkMetadata = () => captureLinkMetadata(ProductShippingProfile);
