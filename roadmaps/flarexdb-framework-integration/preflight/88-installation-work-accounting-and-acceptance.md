# Installation Work Accounting and Acceptance

Status: bounded phase accounting and frozen acceptance are complete within
approved preflight 76. The retained local regression budget is 15 seconds for the
unchanged seventeen-table installation/reopen timer; it is not a deployment SLO. Records 85–87 replace the normal
step and live-resume reconstruction paths.

Neutral coordinator witnesses cover its admitted 7-, 11- and 15-step inventories
on both drivers. A full head read with the optional memo exhausted issues each
receipt and attempt once, as do explicit audit and runtime cold construction for
these inventories. Live resume issues no historical receipt/event evidence.
Protected publication now hands its complete evidence to the existing common
writers and requires one receipt issuance per step. Settled readiness replay
also issues each receipt once and hands its readiness to the availability reader.
Actual availability head/history references and bytes remain freshly validated.
The instrumentation establishes no arbitrary-inventory performance guarantee.

The native witness captures the actual normalized dependency and tail queries
from a normal step and explains them using the restricted installer login. It
records the natural plan separately from forced index eligibility. Existing
indexes cover both the plan/step selection and receipt-storage identity; the
test does not require PostgreSQL to choose a particular equivalent index.

The outcome is an explicit account of normal execution, live reopening,
publication, settled replay, full audit and runtime cold construction, followed
by the frozen end-to-end acceptance matrix. The owner remains persistence's
installation/coordinator code. Adapter semantics, target protection, transaction
boundaries, canonical identities and capability activation do not change.

Measure actual detached rows and canonical bytes, receipt/attempt/event issuance,
and definition/admission restoration independently from elapsed time. Increase
neutral structural inventory and dependency edges; retain the independent attempt,
fork and additive-lineage witnesses. Exercise the full evidence owner with the
optional reference memo exhausted, rather than interpreting a warm memo result
as its algorithmic contract. Check native plans for direct completion and tail
queries and report logical retained graph bounds separately from heap usage.

Positive readiness and runtime cold construction currently retain full evidence
owners. Runtime composition accepts a schema target without installer privilege
authority. Its cold verification must not be replaced by an installer-protection
assumption. A bounded linear full pass at that boundary is acceptable under
preflight 76; an implicit history audit during normal steps is not. The evidence
now accounts for those full boundaries separately; records 89/90 remove the
measured repeated publication and settled-readiness prerequisites.

Retain independent repository readers where they still issue full evidence for
publication, recovery, binding or explicit verification. Delete displaced private
assembly only after tracing its actual consumers. Do not add a cache, mode flag,
alternate authority issuer or diagnostic observer to runtime. Test instrumentation
and acceptance artifacts stay outside production code.

Freeze exact baseline/candidate revisions and identical seventeen-table fixture
inputs in isolated trees. Verify workspace dependency resolution so neither tree
loads the other's runtime source. Alternate three full-suite samples per driver,
retain every sample and apply record 74's unchanged deadlines, median-reduction
and censored-baseline rules. Attribution runs are separate from timing acceptance.
Current Product/SalesChannel/link and Payload preference/content/lifecycle/delete
proofs remain required. ShippingProfile activation remains paused.

Completion requires attributable work bounds, unchanged integrity/recovery
witnesses, native query-plan evidence, the frozen timing matrix, affected checks,
both scoped reviews for retained material changes, resource cleanup, durable
roadmap reconciliation and scoped commits. A passing small plan or diagnostic
timer alone does not complete preflight 76.
