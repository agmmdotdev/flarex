# Installation Redesign Retirement and Acceptance

Status: final cleanup and acceptance gates within approved preflight 76.

Normal execution, live resume, publication and settled replay now have explicit
owners and evidence handoffs. Finish by removing displaced private lookup entry
points, reconciling current domain descriptions, and running the frozen full
seventeen-table comparison. Keep each framework's business and activation gates
separate from structural installation acceptance.

## Retain and retire

| Responsibility | Disposition and evidence |
| --- | --- |
| Actual attempt lineage assembly and exact storage reference restoration | Retain: heads, receipts, terminals and recovery consume these owners. |
| Separate digest-only and lease-identity attempt-reference wrappers | Delete: current event restoration supplies the explicit attempt graph; neither wrapper has a remaining source, test or public-export consumer. Their private reference memo slots retire with them. |
| Assignment restoration registry | Retain: plan and admission restoration still require membership. Delete only its unused exported membership wrapper. |
| Availability-head captured authority | Retain the canonical owner's authority map; delete the unused membership wrapper and redundant WeakSet. Existing writers authenticate through the authority map. |
| Protected publication and independently corroborating repository entry points | Retain distinct prerequisite contracts converging on one actual writer. Attempt abandonment uses the corroborating terminal owner; independent publication fixtures exercise canonical/conflict/reference contracts without duplicating shared domain logic. These are not alternate installation algorithms. |
| Read-only graph reference memo | Retain for independently callable reference readers, with transaction/policy/authority keys, 512-entry limit and cleanup. Full graph issuance witnesses exhaust it; it does not define the graph algorithm. |
| Pure plan/canonical verification scope | Retain bounded byte/frame reuse, no stored or mutable authority, and interruption cleanup. Installer and binding own its distinct lifetimes. |
| Empty displaced import and obsolete roadmap claims | Delete or replace with current owner descriptions. No compatibility format or persistent table is added. |

Removal must preserve current issuers, canonical identities and validation order.
Check whole-workspace references, typecheck, scoped lint and both reviewers; no
new behavioral tests should mirror code that has no caller.

## Final proof boundaries

The successful step reads through installer construction, immutable definition,
fresh locked progress, selected dependencies, fixed structural execution, exact
completion/event/head writes and target-owned settlement. An uncertain step
enters its named recovery operation on an excluded session, fully restores actual
stored evidence and classifies the attempted step; it does not trust the lost
response. Final publication authenticates the complete prefix, inventory and
catalog once, then delegates exact stored writes. Settled readiness freshly reads
availability using its current full head proof. Runtime cold construction keeps
its own schema-only target and full verification.

Structural metadata accounting covers increasing admitted neutral inventories,
dependency edges, independent attempt growth and bounded additive lineage, plus
full reads with the optional memo exhausted. Count transported canonical bytes
and receipt/attempt/event issuance separately. Retained graph positions and
payload bounds are logical memory evidence, not measured peak heap. Native role
and catalog protection remains mandatory on every acquisition; its catalog
query CPU is not included in a claim about structural metadata node complexity.

Freeze isolated revisions with identical Medusa fixture overlays, verify loaded
workspace source paths and keep attribution separate from timing. Apply record
74's alternating three samples per driver, unchanged assertions/deadline,
median-reduction and censored-baseline rules. Derive a bounded local regression
target from those measurements; no arbitrary inventory or deployment SLO follows.
Current Product, Sales Channel, Link/workflow and Currency schema/value contracts,
and Payload preferences/content/lifecycle/delete remain connected proofs.

Completion requires the full frozen comparison and work-reduction evidence,
scoped cleanup, current roadmaps and a coherent commit. Exact revisions, hashes,
samples and validation receipts belong in artifacts and Git history, not a
chronological roadmap log. ShippingProfile's connected workflow and activation
remain paused until their own remaining gates are handled.
